import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {},
    input: {
        height: 50,
        borderWidth: 1,
        borderRadius: 5,
        padding: 10,
        color: '#fff',
    },
});

export default styles;
